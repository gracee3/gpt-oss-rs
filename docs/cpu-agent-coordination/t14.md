# ThinkPad T14 Host Status

- Last update: 2026-08-10T18:27:34-04:00
- Current base commit: `08087869e1c63eaee10605bcd3c4f1fda8392cda`
  (`main`, squash merge of PR #3)
- Worktree: `agent/cpu-serving-mvp`, draft PR #4; this T14 status update only
- Active objective: hold draft PR #4 for the final i7 full-model/API/llama.cpp
  conformance gate, then mark it ready and squash-merge only after that gate passes
- Owned files/surfaces on PR #4: engine CPU worker/configuration, runtime CPU
  planning, server runtime policy/wiring, `README.md`, `docs/CPU_RUNTIME.md`,
  `docs/CPU_I7_CONFORMANCE.md`, and `docs/cpu-agent-coordination/t14.md`
- Published serving commit: `f0f435a4c10f81ea43a5243e5271d5033033fff8`

## Existing follow-up work

- `agent/cpu-serving-mvp` was rebased cleanly as one serving commit onto the
  squash-merged model runtime and published as draft PR #4:
  <https://github.com/gracee3/gpt-oss-rs/pull/4>.
- It implements the real batch-one CPU worker/scheduler, prefill/decode and
  sampling integration, `auto|cpu|cuda|mock` device policy, CPU kernel/thread/
  cache CLI options, the `gpt-oss-cpu` profile, and Chat Completions/Responses
  serving integration.
- PR #3 passed the independent i7 checkpoint gate and all four GitHub jobs, then
  was squash-merged as `0808786`. PR #4 remains draft until the complete i7
  conformance checklist is reported.

## Validation completed on this host

- `cargo fmt --all --check`, `cargo check --workspace --locked`, and
  `cargo test --workspace --locked` pass after rebasing the serving commit onto
  the PR #3 squash merge.
- Forced scalar, AVX2, and AVX-512 VNNI kernel lanes pass on the rebased serving
  branch.
- Forced/detected scalar, AVX2, and AVX-512 VNNI kernel tests pass; BF16 matvec
  and RMS outputs are bit-identical across detected paths.
- Official `openai/gpt-oss-20b` resolved revision:
  `6cee5e81ee83917806bbde320786a8fb61efebee`.
- The generated manifest contains exactly three root SafeTensors shards plus
  tokenizer/config files. The PR #3 fetch selector excludes the redundant
  `original/model.safetensors`; that 13.76 GB duplicate was not downloaded.
- Official checkpoint config/tensor mmap decoding passed.
- Release scalar-vs-auto conformance passed for layer 0 sliding attention + MoE
  and layer 1 full attention + MoE. First repack/conformance peaked near 5.7 GiB
  RSS; the cached gate peaked near 510 MiB RSS.
- Current i5-1145G7 quick BF16 matvec: about 3.19 Gelem/s scalar, 12.92 AVX2,
  and 15.72 AVX-512 VNNI.
- Current MXFP4/Q8 quick results: about 2.00 Gelem/s scalar, 8.32 AVX2, and
  6.67 AVX-512 VNNI. AVX-512 is therefore about 20% slower than AVX2 on this
  T14 as well (less severe than the i7's reported result).
- Both PR #3 workspace jobs and both kernel-dispatch jobs passed before merge.
- Draft PR #4's two workspace and two kernel-dispatch jobs were queued or in
  progress when this status was written.

## Requests for other host

- Fetch the latest `origin/agent/cpu-serving-mvp` and run the complete
  `docs/CPU_I7_CONFORMANCE.md` checklist on the 32 GiB i7.
- Exercise full-model generation plus streaming and non-streaming Chat
  Completions and Responses for plain, longer-context, and Harmony/tool prompts;
  compare deterministic token IDs with the downloaded llama.cpp GGUF.
- Record raw artifact paths, hashes, TTFT, decode rate, peak RSS, cold/warm
  repack timing, and the first divergence or NaN if any in `i7.md`.
- Do not edit the T14-owned serving surfaces listed above.

## Next step

Push this status update, monitor draft PR #4's GitHub jobs, and wait for the i7
final conformance report. Keep PR #4 draft and CPU rejected in trusted mode until
the full gate passes.
