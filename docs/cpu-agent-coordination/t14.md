# ThinkPad T14 Host Status

- Last update: 2026-08-10T17:48:56-04:00
- Current base commit: `4f51eade37c5e181c066c0757c83d1d2fe32a654`
  (`agent/cpu-model-runtime`, PR #3)
- Worktree: this T14 status update only
- Active objective: hold PR #3 for coordinated i7 validation, then squash-merge it
  and publish the existing CPU serving MVP as a draft PR
- Owned files/surfaces on PR #3: `docs/cpu-agent-coordination/t14.md` only
- Owned follow-up surface: local `agent/cpu-serving-mvp` commit `c215420`; engine
  CPU worker/configuration, runtime CPU planning, server runtime policy/wiring,
  `README.md`, `docs/CPU_RUNTIME.md`, and `docs/CPU_I7_CONFORMANCE.md`

## Existing follow-up work

- `agent/cpu-serving-mvp` is a clean local branch with one serving commit atop
  the pre-amend model-runtime commit. It has not been pushed.
- It implements the real batch-one CPU worker/scheduler, prefill/decode and
  sampling integration, `auto|cpu|cuda|mock` device policy, CPU kernel/thread/
  cache CLI options, the `gpt-oss-cpu` profile, and Chat Completions/Responses
  serving integration.
- After PR #3 is squash-merged, the T14 agent will rebase only that serving
  commit onto `origin/main`, re-run the complete workspace gate, and open the
  required draft PR. It will remain draft until the i7 conformance checklist is
  complete.

## Validation completed on this host

- `cargo fmt --all --check`, `cargo check --workspace --locked`, and
  `cargo test --workspace --locked` pass on PR #3.
- Forced/detected scalar, AVX2, and AVX-512 VNNI kernel tests pass; BF16 matvec
  and RMS outputs are bit-identical across detected paths.
- Official `openai/gpt-oss-20b` resolved revision:
  `6cee5e81ee83917806bbde320786a8fb61efebee`.
- The generated manifest contains exactly three root SafeTensors shards plus
  tokenizer/config files. The PR #3 fetch selector excludes the redundant
  `original/model.safetensors`; that 13.76 GB duplicate was not downloaded.
- Official checkpoint config/tensor mmap decoding passed.
- Release scalar-vs-auto conformance passed for layer 0 sliding attention + MoE
  and layer 1 full attention + MoE. First repack/conformance peaked near 5.7 GiB
  RSS; the cached gate peaked near 510 MiB RSS.
- Current i5-1145G7 quick BF16 matvec: about 3.19 Gelem/s scalar, 12.92 AVX2,
  and 15.72 AVX-512 VNNI.
- Current MXFP4/Q8 quick results: about 2.00 Gelem/s scalar, 8.32 AVX2, and
  6.67 AVX-512 VNNI. AVX-512 is therefore about 20% slower than AVX2 on this
  T14 as well (less severe than the i7's reported result).
- After the coordination commit, both new PR #3 kernel-dispatch jobs pass; its
  two new clean-runner workspace jobs are still in progress.

## Requests for other host

- Rebuild/test/benchmark PR #3 at commit `4f51ead` after the downloads finish,
  and record the result in `i7.md`.
- Confirm whether the official native snapshot manifest or manual download on
  the i7 has exactly the same three root SafeTensors shard hashes.
- Do not edit the existing serving surfaces listed above. Please run the full
  model/API/llama.cpp comparison from `docs/CPU_I7_CONFORMANCE.md` after the T14
  agent publishes the serving draft, then record raw artifact paths and summary
  results in `i7.md`.

## Next step

Commit and push this status update, wait for the i7 PR #3 validation/handoff,
then merge PR #3 only after its latest GitHub workspace jobs are also green.
