# i7 Host Status

- Last update: 2026-08-10T17:51:58-04:00
- Current base commit: `c027db3677096c3ce4ce627f164020a260ab9556`
  (`agent/cpu-model-runtime`, PR #3)
- Worktree: this i7 status update only
- Active objective: rebuild, test, benchmark, and run official-checkpoint
  conformance for PR #3 on the 32 GiB i7
- Owned files/surfaces: `docs/cpu-agent-coordination/i7.md` only; no
  implementation surface currently claimed

## Host/toolchain validation

- Installed Rust stable 1.97.1, Cargo, rustfmt, Clippy, rust-analyzer, and Rust
  sources.
- Installed and verified the native build toolchain.
- On the pre-PR3 `agent/cpu-kernels` commit `228d598`, passed
  `cargo fmt --all --check`, `cargo check --workspace --locked`, and
  `cargo test --workspace --locked`.
- On that same pre-PR3 commit, passed forced scalar, AVX2, and AVX-512 VNNI
  CPU-kernel test lanes.
- Captured a pre-PR3 Criterion baseline on the i7-1185G7:
  - BF16 matvec: scalar 689.5 us, AVX2 105.2 us, AVX-512 52.5 us.
  - MXFP4/Q8 dot: scalar 1.842 us, AVX2 424.3 ns, AVX-512 VNNI 815.7 ns.
- PR #3 must be rebuilt, tested, and benchmarked independently on this host;
  the preceding results do not validate its changed kernels/runtime.
- The T14 handoff at `c027db3` is acknowledged. The i7 will not edit the
  serving MVP surfaces claimed in `t14.md`.

## Environment and artifacts

- Native checkpoint download:
  `/data/models/openai/gpt-oss-20b`
- llama.cpp GGUF download:
  `/data/models/llama-cpp/gpt-oss-20b/gpt-oss-20b-MXFP4.gguf`
- Native download: first two root SafeTensors shards complete; third shard is
  downloading.
- GGUF download: 10,577,432,580 of 12,109,566,624 bytes complete (about 87%).
- The branch's native fetch selector currently also selects the redundant
  `original/model.safetensors`; the manual i7 download excludes that extra
  13.76 GB file.

## Requests for other host

- T14 status received: its MXFP4/Q8 AVX-512 VNNI result is also slower than
  AVX2, by about 20% versus about 1.9x on the pre-PR3 i7 baseline.
- Keep the existing unpushed `agent/cpu-serving-mvp` commit and its listed
  engine/server/documentation surfaces T14-owned.
- Notify the i7 after PR #3 is squash-merged and the serving draft is
  published; the i7 will then run `docs/CPU_I7_CONFORMANCE.md` without editing
  those serving surfaces.

## Next step

Run PR #3 format/check/test and kernel benchmarks now. When the native snapshot
finishes, verify the three root shard hashes and run the official checkpoint
gate. Continue monitoring the current GitHub CPU workflow to completion.
