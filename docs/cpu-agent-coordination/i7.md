# i7 Host Status

- Last update: 2026-08-10T17:48:00-04:00
- Current base commit: `5909e275378fbea8e9faecf969be9627f2263e40`
  (PR #3 head before this coordination commit)
- Worktree: coordination documentation only
- Active objective: coordinate a non-overlapping i7 follow-up to PR #3's CPU
  model runtime
- Owned files/surfaces: `docs/cpu-agent-coordination/i7.md` only; no
  implementation surface currently claimed

## Host/toolchain validation

- Installed Rust stable 1.97.1, Cargo, rustfmt, Clippy, rust-analyzer, and Rust
  sources.
- Installed and verified the native build toolchain.
- On the pre-PR3 `agent/cpu-kernels` commit `228d598`, passed
  `cargo fmt --all --check`, `cargo check --workspace --locked`, and
  `cargo test --workspace --locked`.
- On that same pre-PR3 commit, passed forced scalar, AVX2, and AVX-512 VNNI
  CPU-kernel test lanes.
- Captured a pre-PR3 Criterion baseline on the i7-1185G7:
  - BF16 matvec: scalar 689.5 us, AVX2 105.2 us, AVX-512 52.5 us.
  - MXFP4/Q8 dot: scalar 1.842 us, AVX2 424.3 ns, AVX-512 VNNI 815.7 ns.
- PR #3 must be rebuilt, tested, and benchmarked independently on this host;
  the preceding results do not validate its changed kernels/runtime.

## Environment and artifacts

- Native checkpoint download:
  `/data/models/openai/gpt-oss-20b`
- llama.cpp GGUF download:
  `/data/models/llama-cpp/gpt-oss-20b/gpt-oss-20b-MXFP4.gguf`
- Both downloads were active at the last update.
- The branch's native fetch selector currently also selects the redundant
  `original/model.safetensors`; the manual i7 download excludes that extra
  13.76 GB file.

## Requests for other host

- Fill in `t14.md` with the ThinkPad's current commit, unpushed work, claimed
  files, validation, and intended next step before either host starts another
  overlapping implementation task.
- Confirm whether the MXFP4/Q8 AVX-512 VNNI path is also slower than AVX2 on
  the T14. On this i7 it is about 1.9x slower.

## Next step

Validate PR #3 on the i7 after the model downloads finish, while waiting for
the T14 status/claim before choosing an implementation surface.
